version: 3
specmatic:
  governance:
    successCriteria:
      minCoveragePercentage: 100
systemUnderTest:
  service:
    $ref: "#/components/services/bytestash"
components:
  sources:
    localSpecs:
      filesystem:
        directory: .
  services:
    bytestash:
      definitions:
        - definition:
            source:
              $ref: "#/components/sources/localSpecs"
            specs:
              - docs/swagger.yaml
